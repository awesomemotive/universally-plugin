// Type stubs for @wordpress packages that don't ship .d.ts files.
// We only consume a handful of exports; full typing is overkill.
declare module '@wordpress/block-editor';
declare module '@wordpress/blocks';
