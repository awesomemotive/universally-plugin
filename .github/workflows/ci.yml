name: CI

on:
  pull_request:

jobs:
  phpcs:
    name: PHPCS (security + PHP 7.4 compat)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write   # reviewdog needs this to post PR review comments
    steps:
      - uses: actions/checkout@v5

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: composer

      - name: Install phpcs + sniff packages
        run: |
          composer global config --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true
          composer global require --no-progress --no-interaction \
            squizlabs/php_codesniffer:^3 \
            phpcompatibility/phpcompatibility-wp:^2 \
            wp-coding-standards/wpcs:^3 \
            dealerdirect/phpcodesniffer-composer-installer:^1

      - uses: reviewdog/action-setup@v1
        with:
          reviewdog_version: latest

      - name: Run phpcs (phpcs.xml.dist)
        continue-on-error: true
        run: phpcs --report=checkstyle > phpcs-report.xml

      - name: Post findings via reviewdog
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cat phpcs-report.xml | reviewdog \
            -f=checkstyle \
            -name=phpcs \
            -reporter=github-pr-review \
            -filter-mode=nofilter \
            -level=error \
            -fail-on-error=true

  plugin-check:
    name: WordPress Plugin Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: composer

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: plugin/package-lock.json

      - name: Build for plugin-check
        working-directory: plugin
        run: |
          npm ci
          npx wp-scripts build --webpack-src-dir=panel/js --output-path=panel/build
          composer install --no-dev --optimize-autoloader --no-interaction

      - uses: wordpress/plugin-check-action@v1
        with:
          build-dir: ./plugin
          # Disabled checks (rationale per item):
          # - plugin_readme: stable_tag_mismatch is intentional on prereleases; mismatched_plugin_name
          #   is intentional (long SEO name in readme vs short name in header).
          # - plugin_repo: hidden_files fires on .gitignore in source; the dist zip doesn't ship it.
          # - plugin_header_fields: textdomain_mismatch infers slug from the source dir name "plugin/"
          #   which is wrong; trademarked_term likewise.
          # - trademarks: same trademarked_term root cause as above.
          # - non_blocking_scripts / enqueued_scripts_scope: performance warnings on the language
          #   switcher script that we accept.
          exclude-checks: 'plugin_readme,plugin_repo,plugin_header_fields,trademarks,non_blocking_scripts,enqueued_scripts_scope'

  composer-audit:
    name: composer audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: composer

      - name: composer audit
        working-directory: plugin
        run: composer audit --no-dev --abandoned=report

  typescript:
    name: TypeScript type-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: plugin/package-lock.json

      - name: Type-check
        working-directory: plugin
        run: |
          npm ci
          npx tsc --noEmit
